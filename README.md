LLLM-Pipeline is a folder containing the first stage of our pipeline which prompts LLMs with a text-2-sql question.

LLM-Eval is a folder containing the much larger second stage of our pipline which evalutates text-2-sql performance through execution and semantic similarity.
